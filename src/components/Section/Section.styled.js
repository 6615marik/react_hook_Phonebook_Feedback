import styled from '@emotion/styled';

export const Title = styled.h2`
  display: flex;
  flex-direction: column;
  list-style: none;
  text-align: start;
  gap: 10px;
  font-weight: bolder;
  color: rgb(8, 8, 128);
`;
